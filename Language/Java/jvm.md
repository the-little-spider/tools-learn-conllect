jvm
```
	线程共享：
		堆:对象空间
			分代
				新生代1/3：复制算法	Serial 单 
									ParNew 多：追求降低用户停顿时间，适合交互式应用
									Parallel Scavenge 多：追求 CPU 吞吐量，能够在较短时间内完成指定任务，因此适合没有交互的后台计算。											
					Eden区 8/10 
					From Survior 1/10 
					To Survivor 1/10
				老年代2/3：标记-整理算法 或 标记-清除 Serial Old 单, Parallel Old 多, CMS(Concurrent Mark Sweep，并发标记清除)
																						初始标记，并发标记，重新标记，并发清除
			分区
				G1 通用垃圾收集器	
		方法区：1.8元数据区（1.7永久代）永久代在虚拟机内，元数据在本地内存中
			已经被虚拟机加载的类信息
			常量（常量池）
			静态变量
			即时编译器编译后的代码
		
	线程私有：
		本地方法栈
		虚拟机栈：
			多个 栈帧：
					局部变量表：八大原始类型，对象引用，returnAddress
					操作数栈
					动态链接
					方法出口信息
		程序计数器（PC寄存器）
```
